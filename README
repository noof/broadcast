Broadcast: a simple Django app that can send messages to specific GroupMe groups and a Twitter profile at the same time.

"mysite" is a master folder for some Django settings. "polls" and "quiz" are subprojects in the same master folder.
